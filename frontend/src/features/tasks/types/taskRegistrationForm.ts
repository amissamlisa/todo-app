export type TaskRegistrationFormType = {
  goal: string;
  currentStatus: string;
  startDate: string;
  endDate: string;
  weekdayAvailableHours: string;
  holidayAvailableHours: string;
  conditions: string;
};